import { Character } from "@/types/character";

export function Posts({ character }: { character: Character }) {
    return (
        <div className="w-full">
            <p>Posts feed content will go here</p>
        </div>
    );
} 